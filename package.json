{
  "name": "jendie-backend",
  "version": "1.0.0",
  "description": "Backend for Jendie Speed Governor Inventory System (Express + Prisma + PostgreSQL + JWT)",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcrypt": "^5.1.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "prisma": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
